# Back-transform predictor variables from a logit model

Back-transform predictor variables from a logit model

## Usage

``` r
unlogit(p, model)
```

## Arguments

- p:

  probability threshold to unlog from `model`

- model:

  a `glm` model object

## Value

A data frame
